# LearnKana

This is a minimal Flutter Android app for learning hiragana and katakana by typing strict Hepburn romaji answers.

Do not run `flutter build` or `flutter run` unless the user explicitly asks for it.
